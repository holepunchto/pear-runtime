{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "npm run clean && npm run build && ./example.app/Contents/MacOS/example",
    "clean": "rm -rf open example.app",
    "build": "bare-build --host darwin-arm64 --runtime bare-native/runtime index.js"
  },
  "devDependencies": {
    "bare-build": "^0.4.2"
  },
  "build": {
    "appId": "map.app.id",
    "asar": false,
    "mac": {
      "category": "your.app.category.type"
    }
  },
  "dependencies": {
    "bare-native": "^0.1.2",
    "pear-container": "^0.0.3"
  }
}
